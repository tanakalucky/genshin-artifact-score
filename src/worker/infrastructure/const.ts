export const VERSION = process.env.APP_VERSION || "0.0.0-dev";

const CONTACT_EMAIL = process.env.CONTACT_EMAIL || "contact@example.com";
export const USER_AGENT = `Genshin Artifact Score/${VERSION} (${CONTACT_EMAIL})`;
